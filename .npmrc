# Expo/Metro can't resolve pnpm's default isolated (symlinked) node_modules —
# transitive deps like @expo/metro-runtime aren't findable. A hoisted node_modules
# (flat, like npm/yarn) is the Expo-recommended layout for pnpm monorepos.
# See https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
